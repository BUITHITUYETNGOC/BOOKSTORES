@model BOOKSTORE.Models.Revenue

@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    ViewData["Title"] = "Xóa Hóa Đơn";
}
<link href="~/css/sb-admin-2.min.css" rel="stylesheet" />
<link href="~/css/sb-admin-2.css" rel="stylesheet" />
<div class="container-fluid">
    <!-- Page Heading -->
    <h1 class="h3 mb-2 text-gray-800">Quản Lý Doanh Thu</h1>

    <div class="card shadow mb-4">
        <div class="card-body">
            <h3>Bạn có chắc muốn xóa hóa đơn này?</h3>
            <h4>Hóa đơn</h4>
            <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.OrderId) <!-- Use OrderId instead of CartId -->
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.OrderId) <!-- Use OrderId instead of CartId -->
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Total)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Total)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="OrderId" /> <!-- Hidden field for OrderId -->
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
        </form>
        </div>
    </div>
</div>
